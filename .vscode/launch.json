{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "request": "launch",
            "name": "Launch",
            "mainClass": "Driver.Driver",
            "cwd": "${workspaceFolder}/bin",
            "preLaunchTask": "Compile Java"
        }
    ]
}
